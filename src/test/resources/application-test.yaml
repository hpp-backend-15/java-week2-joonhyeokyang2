spring:
  datasource :
    url: jdbc:postgresql://localhost:5432/hhplus
#    driverClassName: org.h2.Driver
    username: postgres
    password:

  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        default_schema: course-test
        #    database-platform: org.hibernate.dialect.H2Dialect

#logging:
#  level:
#    org.hibernate.orm.jdbc.bind: trace
